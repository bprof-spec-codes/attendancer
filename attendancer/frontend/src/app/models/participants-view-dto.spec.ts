import { ParticipantsViewDto } from './participants-view-dto';

describe('ParticipantsViewDto', () => {
  it('should create an instance', () => {
    expect(new ParticipantsViewDto()).toBeTruthy();
  });
});
